# Backend

This directory is intended for the backend server code (e.g., Node.js, Express, database connections).

Currently, the application runs with mock data on the frontend. This is where the real API and database logic will be built.
